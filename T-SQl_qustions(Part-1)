-- Basic level (fresher)
📌 Scenario:
HR wants to check salary of one employee using Employee ID.
👉 Task:
Declare a variable
Store employee salary into variable
Print it
Solution :
declare @Salary int 
select @salary = salary
FROM employees
where emp-id = 1 
print @salary


 2 )If salary > 50000 → Bonus 10%
   Else → Bonus 5%
soln :

Declare @salary int  = 60000
declare @bonus decimal(10,2)
if @salary > 50000
  set @bonus = @salary * 0.10
else
  set @bounus =@salary * 0.05

Declare @salary int 
Declare @bonus Decimal (10,2)
select @salary = salary
from employee
where emp_id = 1 

if @salary > 50000
  set @bounus = @Salary * 0.10
else 
  set @bonus = @salaey * 0.05 

-- Add bonus to salary in table 
update employees
set salary = salary + @bonus
where emp_id = 1 

# professional way :
update employees
set salary = salary +
      case 
          when salary > 50000 then salary * 0.10
          else salary * 0.05 
       end 
where emp_id = 1 













